<?xml version="1.0" encoding="utf-8"?>
<!--
  Release policy: HTTPS everywhere, with two exceptions for the loopback
  addresses a developer's own machine answers on. A campus deployment therefore
  has to terminate TLS — a session token must never cross the college wifi in
  clear.

  Debug builds override this file (see src/debug/res/xml/) so a phone on the
  same wifi can talk to a laptop over plain HTTP while the app is being built.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system"/>
        </trust-anchors>
    </base-config>

    <domain-config cleartextTrafficPermitted="true">
        <!-- The Android emulator's alias for the host machine. -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
